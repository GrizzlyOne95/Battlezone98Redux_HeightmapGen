# Application icon integration

The canonical `branding/repo_icon.svg` is the source artwork for the packaged application icon. The release build and Tk window should use the generated `bzr_heightmap.ico` derived from this artwork.
